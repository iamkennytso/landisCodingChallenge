import React from "react";
import * as styles from './LoadingSpinner.module.scss'

const LoadingSpinner = () => <div className={styles.container}>
  <div className={styles.loadingSpinner} />
</div>

export default LoadingSpinner